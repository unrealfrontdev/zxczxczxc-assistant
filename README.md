# AI Assistant

Универсальный AI-ассистент на базе Tauri + React, объединяющий возможности:
- 💬 Чат с различными LLM (облачными и локальными)
- 🎨 Генерация изображений (Stable Diffusion, DALL-E и др.)
- 📁 Встроенный файловый менеджер с редактором кода
- 📸 Захват экрана и анализ изображений
- 🔍 Веб-поиск прямо из интерфейса
- 📚 Индексация файлов проекта для контекста (RAG)

---

## Системные требования

| Компонент | Версия |
|-----------|--------|
| Node.js   | ≥ 18   |
| npm       | ≥ 9    |
| Rust      | ≥ 1.77 (stable) |
| OS        | Linux, Windows 10+, macOS 11+ |

---

## Установка зависимостей

### Linux (Ubuntu/Debian)

```bash
# Системные библиотеки для Tauri + WebKit
sudo apt update
sudo apt install -y \
  libwebkit2gtk-4.0-dev \
  libssl-dev \
  libgtk-3-dev \
  libayatana-appindicator3-dev \
  librsvg2-dev \
  libxdo-dev \
  libxcb1-dev \
  libxrandr-dev \
  libdbus-1-dev \
  pkg-config \
  build-essential \
  curl
```

> **Wayland (Fedora, Arch, etc.):**  
> Если используете Wayland, установите переменную среды перед запуском:  
> `export GDK_BACKEND=x11 WEBKIT_DISABLE_DMABUF_RENDERER=1`  
> Она уже встроена в `npm run tauri dev`.

### Linux (Fedora/RHEL)

```bash
sudo dnf install -y \
  webkit2gtk4.0-devel \
  openssl-devel \
  gtk3-devel \
  libappindicator-gtk3-devel \
  librsvg2-devel \
  libxdo-devel \
  pkgconf-pkg-config \
  gcc \
  curl
```

### Linux (Arch)

```bash
sudo pacman -S --needed \
  webkit2gtk \
  base-devel \
  curl \
  wget \
  openssl \
  appmenu-gtk-module \
  gtk3 \
  libayatana-appindicator \
  librsvg
```

### Windows

1. Установите [Visual Studio Build Tools 2022](https://visualstudio.microsoft.com/visual-cpp-build-tools/) или полный Visual Studio, включив компонент **"Desktop development with C++"**.
2. Установите [WebView2 Runtime](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) (обычно уже есть в Windows 11).
3. Установите [Node.js LTS](https://nodejs.org/).
4. Установите Rust (шаг ниже).

### macOS

```bash
xcode-select --install
```

---

## Установка Rust

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source "$HOME/.cargo/env"
rustup update stable
```

---

## Быстрый старт

```bash
# 1. Клонировать / перейти в папку проекта
cd ai-assistant

# 2. Установить npm-пакеты
npm install

# 3. Запустить в режиме разработки
npm run tauri dev
```

При первом запуске Rust скомпилирует backend (~2–5 мин). Последующие сборки значительно быстрее.

---

## Сборка релиза

```bash
npm run tauri build
```

Готовый установщик будет в `src-tauri/target/release/bundle/`:
- **Linux**: `.deb`, `.AppImage`
- **Windows**: `.msi`, `.exe`
- **macOS**: `.dmg`, `.app`

---

## Настройка LLM-провайдеров

В интерфейсе откройте **Settings** (шестерёнка) → вкладка **AI Provider**.

### Облачные провайдеры

| Провайдер | Где взять API ключ | Пример модели |
|-----------|-------------------|---------------|
| **OpenAI** | https://platform.openai.com/api-keys | `gpt-4o` |
| **Anthropic Claude** | https://console.anthropic.com/settings/keys | `claude-3-5-sonnet-20241022` |
| **DeepSeek** | https://platform.deepseek.com/api_keys | `deepseek-chat` |
| **OpenRouter** | https://openrouter.ai/keys | `openai/gpt-4o` |

Вставьте ключ в поле **API Key**, выберите модель и нажмите **Save**.

### Локальные провайдеры

#### LM Studio

1. Скачайте [LM Studio](https://lmstudio.ai/) и загрузите любую модель.
2. Запустите локальный сервер: **Local Server → Start Server** (порт по умолчанию `1234`).
3. В настройках выберите провайдер **LM Studio**, URL: `http://localhost:1234`.
4. Нажмите кнопку **🔍 Detect models** — список моделей заполнится автоматически.

#### Ollama

1. Установите [Ollama](https://ollama.ai/):
   ```bash
   # Linux / macOS
   curl -fsSL https://ollama.ai/install.sh | sh
   
   # Загрузить модель (пример)
   ollama pull llama3.2
   ollama pull mistral
   ```
2. Ollama запускается автоматически на `http://localhost:11434`.
3. В настройках выберите **Ollama**, URL: `http://localhost:11434`.
4. Нажмите **🔍 Detect models** для автоопределения.

#### Универсальный OpenAI-совместимый сервер

Подходит для любого сервера с `/v1/chat/completions` API (llama.cpp server, vLLM, text-generation-webui и др.):
- Провайдер: **LM Studio**
- URL: адрес вашего сервера (например `http://localhost:8080`)

---

## Настройка генерации изображений

В настройках откройте вкладку **Image Generation**.

### Stable Diffusion WebUI (AUTOMATIC1111)

1. Установите [stable-diffusion-webui](https://github.com/AUTOMATIC1111/stable-diffusion-webui).
2. Запустите с флагом API:
   ```bash
   # Linux / macOS
   ./webui.sh --api
   
   # Windows
   webui-user.bat --api
   ```
3. WebUI будет доступна на `http://localhost:7860`.
4. В настройках выберите провайдер **Stable Diffusion (local)**, укажите URL `http://localhost:7860`.
5. Нажмите **📋 Load models from server** для загрузки списка моделей.

### ComfyUI

1. Установите [ComfyUI](https://github.com/comfyanonymous/ComfyUI) и запустите его (порт `8188` по умолчанию).
2. Провайдер **ComfyUI**, URL `http://localhost:8188`.

### DALL-E (OpenAI)

- Провайдер: **DALL-E**
- API Key: ключ OpenAI (тот же, что для GPT)
- Модель: `dall-e-3` или `dall-e-2`

### Другие облачные провайдеры

| Провайдер | URL / Ключ |
|-----------|-----------|
| **Stability AI** | https://platform.stability.ai/account/keys |
| **Replicate** | https://replicate.com/account/api-tokens |

---

## Настройка веб-поиска

В настройках вкладка **Web Search**.

- **DuckDuckGo** — работает без ключа, включено по умолчанию.
- **Brave Search** — более качественные результаты:
  1. Получите API ключ: https://api.search.brave.com/
  2. Вставьте в поле **Brave API Key**.

---

## Горячие клавиши

| Действие | Горячая клавиша |
|----------|----------------|
| Показать/скрыть окно | `Ctrl+Shift+Space` (глобальная) |
| Захват экрана | `Ctrl+Shift+S` (глобальная) |
| Отправить сообщение | `Ctrl+Enter` |
| Отменить генерацию | кнопка **■ Stop** |

> Глобальные горячие клавиши работают даже когда приложение свёрнуто.

---

## Файловый менеджер (вкладка Files)

- **Открыть папку**: нажмите кнопку 📁 в верхней части панели для выбора корневой директории.
- **Создать файл**: наведите курсор на папку → нажмите `+f`.
- **Создать папку**: наведите курсор на папку → нажмите `+d`.
- **Переименовать**: двойной клик по имени элемента.
- **Удалить**: нажмите `✕` при наведении.
- **Редактировать**: клик по файлу открывает встроенный редактор.
- **Сохранить**: `Ctrl+S` в редакторе.
- **Индексировать для AI**: кнопка **Index** добавляет файлы проекта в контекст чата.

---

## Переменные среды (опционально)

Создайте файл `.env` в корне `ai-assistant/` для задания значений по умолчанию:

```env
# Не обязательно — все настройки можно задать через UI
VITE_DEFAULT_PROVIDER=openai
```

> **Безопасность**: Никогда не коммитьте API ключи. Все ключи хранятся только в localStorage браузерного движка Tauri.

---

## Устранение проблем

### Ошибка компиляции Rust: `pkg-config not found`

```bash
# Linux
sudo apt install pkg-config libssl-dev

# Fedora
sudo dnf install pkgconf-pkg-config openssl-devel
```

### Белый экран при запуске (Linux / Wayland)

```bash
export GDK_BACKEND=x11
export WEBKIT_DISABLE_DMABUF_RENDERER=1
npm run tauri dev
```

### LM Studio / Ollama не обнаруживаются

Убедитесь, что сервер запущен **до** нажатия кнопки Detect. Проверьте CORS:
- **LM Studio**: Settings → Server → Enable CORS ✓
- **Ollama**: по умолчанию CORS разрешён для localhost

### Ошибка `error[E0463]: can't find crate` при первой сборке

```bash
rustup update stable
cargo clean   # внутри src-tauri/
```

### macOS: `Library not loaded: libwebkit2gtk`

На macOS используется системный WebKit, дополнительных библиотек не нужно. Убедитесь, что установлены Xcode Command Line Tools:
```bash
xcode-select --install
```

---

## Структура проекта

```
ai-assistant/
├── src/                    # React + TypeScript фронтенд
│   ├── components/
│   │   ├── AssistantPanel.tsx   # Главная панель (чат / файлы / изображения)
│   │   ├── ApiKeyInput.tsx      # Настройки LLM провайдеров
│   │   ├── ImageGenSettings.tsx # Настройки генерации изображений
│   │   ├── FileTree.tsx         # Файловый менеджер
│   │   ├── ImageGallery.tsx     # Галерея сгенерированных изображений
│   │   ├── MarkdownRenderer.tsx # Рендеринг Markdown с подсветкой синтаксиса
│   │   ├── FileEditBlock.tsx    # Блоки редактирования файлов в ответах AI
│   │   ├── ChatHistory.tsx      # История чата
│   │   ├── CharacterImport.tsx  # Импорт карточек персонажей (SillyTavern)
│   │   └── WebSearchToggle.tsx  # Переключатель веб-поиска
│   └── store/
│       └── assistantStore.ts    # Zustand store (состояние приложения)
└── src-tauri/              # Rust backend
    └── src/
        ├── main.rs              # Точка входа + регистрация команд
        ├── ai_bridge.rs         # HTTP-клиенты LLM + стриминг SSE
        ├── project_indexer.rs   # RAG индексация + файловые операции
        ├── screen_capture.rs    # Захват экрана
        ├── overlay.rs           # Оконный оверлей
        ├── clipboard.rs         # Работа с буфером обмена
        └── web_search.rs        # Веб-поиск
```

---

## Лицензия

MIT
